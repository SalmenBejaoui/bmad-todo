{
  "name": "bmad-todo",
  "private": true,
  "workspaces": ["frontend", "backend", "e2e"],
  "scripts": {
    "dev": "concurrently \"npm -w frontend run dev\" \"npm -w backend run dev\"",
    "test": "npm -w backend test && npm -w frontend test",
    "test:e2e": "npm -w e2e test",
    "build": "npm -w frontend run build && npm -w backend run build"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
